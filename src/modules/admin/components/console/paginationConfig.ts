// Shared pagination constants, kept out of the component file so fast
// refresh stays happy (a file should export only components or only
// non-components).
export const PAGE_SIZES = [10, 20, 50, 100];
export const DEFAULT_PAGE_SIZE = 10;
